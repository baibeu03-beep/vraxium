"use client";

// Phalanx 전용 cluster-9 변형. 원본 /cluster-9 는 손대지 않는다.
import ClusterPlaceholderPx from "@/components/shared/ClusterPlaceholderPx";

const Cluster9PxPage = () => {
  return <ClusterPlaceholderPx />;
};

export default Cluster9PxPage;
