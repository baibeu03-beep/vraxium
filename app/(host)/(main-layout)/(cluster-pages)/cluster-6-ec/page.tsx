"use client";

// Encre 전용 cluster-6 변형. 원본/PX 무영향 — 상세는 cluster-5-ec/page.tsx 참고.
import ClusterPlaceholderEc from "@/components/shared/ClusterPlaceholderEc";

const Cluster6EcPage = () => {
  return <ClusterPlaceholderEc />;
};

export default Cluster6EcPage;
