"use client";

// Phalanx 전용 cluster-10 변형. 원본 /cluster-10 는 손대지 않는다.
import ClusterPlaceholderPx from "@/components/shared/ClusterPlaceholderPx";

const Cluster10PxPage = () => {
  return <ClusterPlaceholderPx />;
};

export default Cluster10PxPage;
