"use client";

// Phalanx 전용 cluster-7 변형. 원본 /cluster-7 는 손대지 않는다.
import ClusterPlaceholderPx from "@/components/shared/ClusterPlaceholderPx";

const Cluster7PxPage = () => {
  return <ClusterPlaceholderPx />;
};

export default Cluster7PxPage;
