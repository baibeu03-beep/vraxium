"use client";

// Encre 전용 cluster-8 변형. 원본/PX 무영향.
import ClusterPlaceholderEc from "@/components/shared/ClusterPlaceholderEc";

const Cluster8EcPage = () => {
  return <ClusterPlaceholderEc />;
};

export default Cluster8EcPage;
