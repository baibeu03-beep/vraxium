"use client";

// Phalanx 전용 cluster-8 변형. 원본 /cluster-8 는 손대지 않는다.
import ClusterPlaceholderPx from "@/components/shared/ClusterPlaceholderPx";

const Cluster8PxPage = () => {
  return <ClusterPlaceholderPx />;
};

export default Cluster8PxPage;
